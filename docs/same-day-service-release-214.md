# Same-day service accounting candidate

Migration 202609090214 replaces the service-day preview helper. A checked-out stay qualifies for same-day service only when it has an assigned physical room, a non-null check-in, a later checkout timestamp, and both timestamps fall on the service date in the service book's timezone. Existing pricing validation, exact charge components, close-time gates, scoped access and concurrency hashes remain in force.

The same-day row retains zero occupied overnight nights. Its allocation carries `service_usage: same_day_use`, which the existing close routine preserves in entry details. Ordinary overnight and unoccupied rows receive their respective classification. The entire reviewed row participates in the preview hash. No old closed entries or folio charges are rewritten.

This resolves allocation of an actual same-day stay against an existing reservation. It does not add a new same-date arrival/departure booking product, invent occupancy, allocate revenue to unused later nights, or determine a property's tax treatment. Cancellations and unresolved arrivals retain their existing rules.

Validation runner: `work/accounting-foundation/native-same-day-service-test.mjs` in the parent workspace. Covers hotel/home close and replay, preserved component amounts, overnight comparison, missing room/check-in, invalid timestamp order, cancellations, unresolved arrivals, prior-day checkout and unfinished-date rejection. Run against an isolated local database, never hosted fixtures.

Status: local candidate, not deployed. After the existing local PostgreSQL recovery finished, the runner installed all PMS migrations through 213 and the new 214 candidate. Twenty recorded hotel/home scenarios passed, including actual v2 close persistence and idempotent replay; fixture transactions rolled back. Result: `work/accounting-foundation/same-day-service-test-result.json` in the parent workspace. The earlier connection failures were startup failures, not test passes.

The Site candidate now renders service use in the close preview and adds Service use / Occupied overnight columns to the shared closed-report rows used by screen, print and CSV. Older entries without usage metadata retain a neutral occupancy label. The full Site build and type-aware lint for the three changed files pass. No new hooks, data fetching, dependencies or rendering side effects were added.

Follow-up validation now passes 24 recorded scenarios. Hotel and home same-day sources each post to the GL with accommodation credit 10000, aggregate fee credit 2500, aggregate tax credit 1430 and receivable debit 13930 minor units. Independent trial balances and source reconciliation agree; duplicate posting replays without changing the immutable source. Separate saved-quote fixtures retain city 150, state 780, lodging 500, resort 2000 and technology 500 minor units through actual preview and close, with zero overnight occupancy. The saved quotes are local integration fixtures, not evidence of a newly exercised hosted quote-creation flow. Itemized-to-GL mapping remains a separate verification from the aggregate journal checks.

Itemized GL follow-up passes for both property models: six separate credit accounts retain accommodation 10000, city 150, state 780, lodging 500, resort 2000 and technology 500 minor units, balanced by receivable debit 13930. Each journal has exactly seven lines and sums to zero. The full suite now records 26 passing scenarios. Actual service report RPC results were captured before rolling back the fixtures in `same-day-hotel-report-fixture.json` and `same-day-home-report-fixture.json`; these provide database-generated, fictional report inputs for browser verification.

Browser display acceptance completed using the actual ClosedServiceReports component in the explicitly labeled local read-only harness at 127.0.0.1:4350. Hotel and home display their distinct reservation and close references, $100 accommodation, $1.50 city, $7.80 state, $5 lodging, $20 resort, $5 technology, $125 revenue, $139.30 total, and Same-day service (no overnight) / No. The property switch remounts the report. No browser warning/error logs were captured. Evidence: `same-day-hotel-browser.txt` and `same-day-home-browser.txt` in the accounting-foundation directory. The report row builder is shared with CSV and print; this display test does not prove actual download/printing or server-side date filtering. The harness returns saved test data only and cannot write to a hotel.

Close-preview presentation acceptance also passes for hotel and home using the actual ServiceDayPanel and captured database previews. Both show $139.30, Same-day service (no overnight), zero occupied nights and Ready for review for the completed historical test date. Clicking close with its checkbox unchecked leaves the required input invalid and produces no pending request/error; no test write endpoint is exposed. Browser warning/error logs were empty. Evidence: `same-day-close-preview-hotel.txt` and `same-day-close-preview-home.txt`.

Remaining before release: hosted migration installation and Site publication, and hosted acceptance after the service day finishes. Actual downloaded/printed report delivery remains in the launch checklist. The existing local application at localhost:4330 loads the PMS sign-in screen; no local owner session was available to enter its authenticated reports. The isolated harness does not prove production authentication. No current-day gate is bypassed and no hosted data has been modified by this candidate.
