# Use a stable HTTPS Preview alias in deployed Preview environments so secured
# background workers wake the same isolated deployment instead of Production.
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_URL=
# Preferred for new Supabase projects:
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Legacy projects may use this instead:
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# Server-only 32-byte base64 key for AES-256-GCM PMS credential encryption.
PMS_CREDENTIAL_ENCRYPTION_KEY=
# Oracle OPERA Cloud Distribution Shop/Book credentials. These are separate
# from OHIP Property API credentials and are issued during channel onboarding.
PMS_ORACLE_OPERA_DISTRIBUTION_BASE_URL=
PMS_ORACLE_OPERA_DISTRIBUTION_TOKEN_URL=
PMS_ORACLE_OPERA_DISTRIBUTION_USERNAME=
PMS_ORACLE_OPERA_DISTRIBUTION_PASSWORD=
PMS_ORACLE_OPERA_DISTRIBUTION_APP_KEY=
PMS_ORACLE_OPERA_DISTRIBUTION_CHANNEL_CODE=
PMS_ORACLE_OPERA_DISTRIBUTION_ORIGIN=iRatePilot
PMS_ORACLE_OPERA_DISTRIBUTION_TIMEOUT_MS=15000
# Bearer credential agreed with Oracle for outbound ARI/notification delivery.
PMS_ORACLE_OPERA_WEBHOOK_SECRET=
# OHIP Property API credentials and approved hotel scope.
PMS_ORACLE_OPERA_BASE_URL=
PMS_ORACLE_OPERA_TOKEN_URL=
PMS_ORACLE_OPERA_CLIENT_ID=
PMS_ORACLE_OPERA_CLIENT_SECRET=
PMS_ORACLE_OPERA_APP_KEY=
PMS_ORACLE_OPERA_HOTEL_ID=
PMS_ORACLE_OPERA_TIMEOUT_MS=15000
# Brand/vendor-issued booking endpoints. Never infer or guess these paths.
PMS_HILTON_PEP_BASE_URL=
PMS_HILTON_PEP_API_CREDENTIAL=
PMS_HILTON_PEP_AVAILABILITY_PATH=
PMS_HILTON_PEP_CREATE_PATH=
PMS_HILTON_PEP_GET_PATH=
PMS_HILTON_PEP_MODIFY_PATH=
PMS_HILTON_PEP_CANCEL_PATH=
PMS_HILTON_PEP_VALIDATION_PATH=
PMS_HILTON_PEP_WEBHOOK_SECRET=
PMS_HILTON_ONQ_BASE_URL=
PMS_HILTON_ONQ_API_CREDENTIAL=
PMS_HILTON_ONQ_AVAILABILITY_PATH=
PMS_HILTON_ONQ_CREATE_PATH=
PMS_HILTON_ONQ_GET_PATH=
PMS_HILTON_ONQ_MODIFY_PATH=
PMS_HILTON_ONQ_CANCEL_PATH=
PMS_HILTON_ONQ_VALIDATION_PATH=
PMS_HILTON_ONQ_WEBHOOK_SECRET=
PMS_MARRIOTT_FOSSE_BASE_URL=
PMS_MARRIOTT_FOSSE_API_CREDENTIAL=
PMS_MARRIOTT_FOSSE_AVAILABILITY_PATH=
PMS_MARRIOTT_FOSSE_CREATE_PATH=
PMS_MARRIOTT_FOSSE_GET_PATH=
PMS_MARRIOTT_FOSSE_MODIFY_PATH=
PMS_MARRIOTT_FOSSE_CANCEL_PATH=
PMS_MARRIOTT_FOSSE_VALIDATION_PATH=
PMS_MARRIOTT_FOSSE_WEBHOOK_SECRET=
PMS_MARRIOTT_FS_PMS_BASE_URL=
PMS_MARRIOTT_FS_PMS_API_CREDENTIAL=
PMS_MARRIOTT_FS_PMS_AVAILABILITY_PATH=
PMS_MARRIOTT_FS_PMS_CREATE_PATH=
PMS_MARRIOTT_FS_PMS_GET_PATH=
PMS_MARRIOTT_FS_PMS_MODIFY_PATH=
PMS_MARRIOTT_FS_PMS_CANCEL_PATH=
PMS_MARRIOTT_FS_PMS_VALIDATION_PATH=
PMS_MARRIOTT_FS_PMS_WEBHOOK_SECRET=
PMS_HOTELKEY_BASE_URL=
PMS_HOTELKEY_API_CREDENTIAL=
PMS_HOTELKEY_AVAILABILITY_PATH=
PMS_HOTELKEY_CREATE_PATH=
PMS_HOTELKEY_GET_PATH=
PMS_HOTELKEY_MODIFY_PATH=
PMS_HOTELKEY_CANCEL_PATH=
PMS_HOTELKEY_VALIDATION_PATH=
PMS_HOTELKEY_WEBHOOK_SECRET=
# Production/non-flight use only. It must be absent from the isolated Flight
# Consumer Preview deployment, whose runtime accepts only its restricted key.
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
# Dedicated Consumer Flight Preview restricted TEST key and webhook. Keep all
# of these Preview-only. The key must start rk_test_; pin its exact SHA-256.
FLIGHT_CONSUMER_PREVIEW_STRIPE_RESTRICTED_KEY=
FLIGHT_CONSUMER_PREVIEW_STRIPE_RESTRICTED_KEY_SHA256=
NEXT_PUBLIC_FLIGHT_CONSUMER_PREVIEW_STRIPE_PUBLISHABLE_KEY=
FLIGHT_CONSUMER_PREVIEW_STRIPE_PUBLISHABLE_KEY_SHA256=
# The signing secret must differ from STRIPE_WEBHOOK_SECRET.
FLIGHT_CONSUMER_PREVIEW_STRIPE_WEBHOOK_SECRET=
FLIGHT_CONSUMER_PREVIEW_STRIPE_ACCOUNT_SHA256=
# Optional when Stripe emits account-scoped webhook events.
FLIGHT_CONSUMER_PREVIEW_STRIPE_ACCOUNT_ID=
# Dedicated Duffel test webhook signing secret returned only when the test
# endpoint is created. Never share it with Production or the authority secret.
FLIGHT_CONSUMER_PREVIEW_DUFFEL_WEBHOOK_SECRET=
# Stripe recurring prices must use yearly billing: Basic $70/year and Business $120/year.
STRIPE_BASIC_PRICE_ID=
STRIPE_BUSINESS_PRICE_ID=
STRIPE_PARTNER_STARTER_PRICE_ID=
STRIPE_PARTNER_PROFESSIONAL_PRICE_ID=
STRIPE_PARTNER_PREMIUM_PRICE_ID=
OPENAI_API_KEY=
RESEND_API_KEY=
# Verified sender used for transactional booking and partner messages.
RESEND_FROM_EMAIL=
# Signing secret for /api/webhooks/resend. Keep server-only.
RESEND_WEBHOOK_SECRET=
# Optional address for customer and partner replies.
EMAIL_REPLY_TO=
# Required by Vercel Cron and any manual email worker invocation.
CRON_SECRET=
# Safety hold: only the exact value true permits the worker to claim or send jobs.
EMAIL_WORKER_ENABLED=false
AUTOMATION_POLICY_SCANNER_ENABLED=false
AUTOMATION_SANDBOX_EXECUTOR_ENABLED=false
# Maximum email jobs processed per worker invocation (1-25).
EMAIL_WORKER_BATCH_SIZE=10
# Optional server-only destination for structured operational alerts.
ERROR_WEBHOOK_URL=
ERROR_WEBHOOK_TOKEN=
NEXT_PUBLIC_PUBLIC_BOOKING=false
NEXT_PUBLIC_ENABLE_TEST_CHECKOUT=false
ENABLE_TEST_CHECKOUT=false
# Commercial launch only: requires sk_live_/pk_live_ keys, public booking=true,
# PILOT_MODE=false, and a separately enabled live webhook endpoint.
ENABLE_LIVE_BOOKING_PAYMENTS=false
# Live Stripe Connect onboarding and guarded partner transfers (requires verified production migration 029 before activation).
ENABLE_LIVE_PARTNER_PAYOUTS=false
ENABLE_LIVE_STRIPE_WEBHOOKS=false
PILOT_MODE=true
# Consumer flight checkout remains an exact Preview/test-only capability.
FLIGHT_CONSUMER_PREVIEW_ENABLED=false
FLIGHT_RUNTIME_MODE=synthetic
FLIGHT_RUNTIME_ENVIRONMENT=preview
FLIGHT_RUNTIME_ENABLED=false
FLIGHT_SYNTHETIC_ADAPTER_ENABLED=true
FLIGHT_PROVIDER_TRAFFIC_ENABLED=false
FLIGHT_BOOKING_ENABLED=false
FLIGHT_PAYMENT_ENABLED=false
FLIGHT_SETTLEMENT_ENABLED=false
FLIGHT_TICKETING_ENABLED=false
FLIGHT_SERVICING_ENABLED=false
FLIGHT_WEBHOOKS_ENABLED=false
FLIGHT_PRODUCTION_TRAFFIC_ENABLED=false
FLIGHT_TRANSACTION_KILL_SWITCH=engaged
# Canonical server-only payment execution binding. Populate all five together;
# partial bindings are invalid and no value grants payment authority by itself.
FLIGHT_PAYMENT_PROCESSOR_ID=
FLIGHT_PAYMENT_ADAPTER_VERSION=
FLIGHT_PAYMENT_ADAPTER_SOURCE_SHA256=
FLIGHT_PAYMENT_ACCOUNT_SCOPE_SHA256=
FLIGHT_PAYMENT_ENVIRONMENT_SCOPE_SHA256=
DUFFEL_TEST_ACCESS_TOKEN=
FLIGHT_DUFFEL_TEST_AUTHORITY_SECRET=
# Consumer Production dark launch. The live token and signing secret must be
# sensitive Vercel variables scoped to Production only. The account digest is
# computed with the frozen domain from the exact approved Duffel account
# identifier below. The credential digest is the domain-separated fingerprint
# of the exact approved live token; all three remain server-only and the dark
# gate fails closed when any binding is absent or mismatched.
DUFFEL_LIVE_ACCESS_TOKEN=
FLIGHT_CONSUMER_PRODUCTION_DUFFEL_ACCOUNT_ID=
FLIGHT_CONSUMER_PRODUCTION_DUFFEL_ACCOUNT_SHA256=
FLIGHT_CONSUMER_PRODUCTION_DUFFEL_CREDENTIAL_SHA256=
FLIGHT_CONSUMER_PRODUCTION_DUFFEL_WEBHOOK_SECRET=
FLIGHT_CONSUMER_PRODUCTION_DARK_ENABLED=false
FLIGHT_CONSUMER_PRODUCTION_RELEASE_ENABLED=false
# Admin-only live inventory diagnostics. The refresh-observation lane accepts a
# raw offer ID only ephemerally, persists digests only, permits at most one GET,
# and is not authoritative checkout pricing. Keep both lanes default-off;
# orders, payments, tickets, and consumer release remain disabled.
FLIGHT_CONSUMER_PRODUCTION_DUFFEL_SHOPPING_DARK_ENABLED=false
FLIGHT_CONSUMER_PRODUCTION_DUFFEL_LIVE_REPRICE_DARK_ENABLED=false
FLIGHT_CONSUMER_PRODUCTION_DUFFEL_SHOPPING_ORDER_ENABLED=false
FLIGHT_CONSUMER_PRODUCTION_DUFFEL_ORDER_PLAN_REHEARSAL_ENABLED=false
# Code-only Production public-shopping prerequisite. Even when true, this gate
# grants no public exposure, provider dispatch, persistence, order, payment, or
# release capability. Policy, cohort, and deployed source must be hash-bound.
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_SHOPPING_PREVIEW_ENABLED=false
# Admission-only budget reservation. This remains non-dispatching and is not
# used by any public route; enabling it does not authorize Duffel traffic.
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_SHOPPING_ADMISSION_ENABLED=false
# Route-free, one-operation Duffel offer-request dispatch. This composes the
# admitted public-shopping scope, remains server-only/default-off, and grants
# no consumer exposure, order, payment, capture, ticketing, or release ability.
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_SHOPPING_DISPATCH_ENABLED=false
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_SHOPPING_POLICY_SHA256=
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_SHOPPING_COHORT_SHA256=
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_SHOPPING_SOURCE_COMMIT_SHA=
# Route-free Gate 117 offer-reference encryption and bounded retention purge.
# Both are default-off. Supply distinct canonical base64url 32-byte keys through
# the Production secret store only; placeholders below contain no key material.
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_OFFER_REFERENCE_ENCRYPTION_ENABLED=false
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_OFFER_REFERENCE_ENCRYPTION_KEY_BASE64URL=
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_OFFER_REFERENCE_HMAC_KEY_BASE64URL=
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_OFFER_REFERENCE_KEY_VERSION=
FLIGHT_CONSUMER_PRODUCTION_PUBLIC_OFFER_REFERENCE_PURGE_ENABLED=false
# Admin-only read-only Stripe platform-account preflight. These approval
# digests bind the exact live account, restricted read-only server credential
# (rk_live_ only), and browser
# publishable key. This gate permits one GET /v1/account only and never
# authorizes a PaymentIntent, charge, refund, webhook, order, ticket, or release.
# Keep these dedicated Production-only values isolated from Preview and from
# the application's generic Stripe test credentials.
FLIGHT_CONSUMER_PRODUCTION_STRIPE_ACCOUNT_PREFLIGHT_ENABLED=false
FLIGHT_CONSUMER_PRODUCTION_STRIPE_ACCOUNT_PREFLIGHT_KEY=
FLIGHT_CONSUMER_PRODUCTION_STRIPE_PUBLISHABLE_KEY=
FLIGHT_CONSUMER_PRODUCTION_STRIPE_ACCOUNT_SHA256=
FLIGHT_CONSUMER_PRODUCTION_STRIPE_CREDENTIAL_SHA256=
FLIGHT_CONSUMER_PRODUCTION_STRIPE_PUBLISHABLE_KEY_SHA256=
# Server-only, zero-dispatch Stripe PaymentIntent plan journaling. This is a
# separate dark gate: it requires the canonical FLIGHT_PAYMENT_* binding, keeps
# every Stripe transport/payment/release capability false, and has no route.
FLIGHT_CONSUMER_PRODUCTION_STRIPE_PAYMENT_PLAN_DARK_ENABLED=false
FLIGHT_CONSUMER_PREVIEW_PII_KEY_VERSION=
FLIGHT_CONSUMER_PREVIEW_PII_ENCRYPTION_KEY_BASE64URL=
FLIGHT_CONSUMER_PREVIEW_PII_HMAC_KEY_BASE64URL=
FLIGHT_CONSUMER_PREVIEW_EVIDENCE_KEY_VERSION=
FLIGHT_CONSUMER_PREVIEW_EVIDENCE_ENCRYPTION_KEY_BASE64URL=
FLIGHT_CONSUMER_PREVIEW_EVIDENCE_HMAC_KEY_BASE64URL=
FLIGHT_CONSUMER_PREVIEW_REFERENCE_KEY_VERSION=
FLIGHT_CONSUMER_PREVIEW_REFERENCE_ENCRYPTION_KEY_BASE64URL=
FLIGHT_CONSUMER_PREVIEW_REFERENCE_HMAC_KEY_BASE64URL=
# Mobile push delivery remains off until migration 030, EAS credentials, and physical-device QA are complete.
ENABLE_MOBILE_PUSH_NOTIFICATIONS=false
# Optional server-only Expo access token when enhanced push security is enabled in EAS.
EXPO_ACCESS_TOKEN=
