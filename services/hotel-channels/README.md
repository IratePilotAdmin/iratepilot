# Hotel channel adapters

## Booking.com: development-only ARI preparation and test transport

`booking-com/ari.ts` converts explicit room-availability and nightly-rate deltas into Booking.com OTA 2003B XML requests. It groups each request by one mapped Booking.com property, update type, and calendar month; rejects duplicate or out-of-range updates; preserves the selected tax basis; and makes no network calls.

`booking-com/client.ts` adds a guarded **test harness transport**. It accepts only explicitly approved test-property scope, an injected fetcher, and a caller-supplied bearer token; checks the exact provider endpoints; rejects redirects; bounds response size; and parses basic success/error/warning acknowledgements. It has no default network transport and is not wired to the app, a token exchange, a durable queue, booking import, or production configuration. Tests use an in-memory fake response only; they do not contact Booking.com.

`booking-com/reservations.ts` describes the provider's new-reservation and modified/cancelled queue polls, plus per-message success/failure acknowledgements. Its test client likewise requires a caller-injected transport, test-only property scope, reservation access and PII compliance gates. A success acknowledgement cannot be constructed until the caller confirms durable persistence; reservation response bodies are capped and are not logged or retained by this client. The parser and encrypted inbox now provide separate parsing and staging primitives, but they are not wired to this poller or to PMS reservation creation, and scheduled polling is not implemented.

`booking-com/reservation-parser.ts` validates bounded OTA XML and normalizes provider IDs, property/room/rate mapping IDs, stay dates, guest counts, totals, and the lead guest's basic name/contact fields. It ignores unrelated XML fields, including payment-card fields, in its returned value. The parser does not persist the normalized guest data; an atomic PMS reservation/folio transaction is still required before a success acknowledgement may be sent.

`booking-com/inbox.ts` encrypts the normalized reservation with AES-256-GCM and property/connection/event-bound associated data, hashes the provider reservation ID for replay detection, then submits only the ciphertext and hashes to the service-only inbox RPC. It fails closed unless `OTA_RESERVATION_PII_ENCRYPTION_KEY` is configured as a base64-encoded 32-byte key. The accompanying migration installs disabled test-only channel metadata, room mappings, encrypted inbox storage, duplicate detection, and server-only staging. The staged record remains `received`; staging is **not** equivalent to creating a PMS booking and cannot yet be acknowledged as processed.

This external-provider inbox is separate from the native iRatePilot.com reservation outbox described in `docs/IRATEPILOT-NATIVE-PMS-CONNECTOR.md`. Do not reuse `PMS_CREDENTIAL_ENCRYPTION_KEY` for guest PII. Configure the dedicated key only in the isolated test secret store after approving the target; never commit key material or add it to browser-visible variables.

This is still not a usable Booking.com connector. There is no account credential storage/token refresh, atomic PMS booking/folio import, reservation modification/cancellation application, data-retention worker, deployed scheduled worker/API integration, property mapping UI, or provider certification evidence. The public capability route must not report an external OTA as connected or production-ready until those flows are implemented and verified.

Booking.com requires a Connectivity Partner relationship and property-granted connection scopes before a provider can manage a property's data. Rates/availability and reservations are separate connection types. Current authentication uses property-scoped machine accounts and short-lived bearer tokens; endpoint permissions and any required certification must also be in place. New reservations use `GET` then `POST OTA_HotelResNotif`; changed/cancelled reservations use the corresponding `OTA_HotelResModifyNotif` flow. Reservations require PII compliance approval. See the [Connectivity API overview](https://developers.booking.com/connectivity/docs), [authentication](https://developers.booking.com/connectivity/docs/authentication), [token authentication](https://developers.booking.com/connectivity/docs/token-based-authentication), [endpoint access requirements](https://developers.booking.com/connectivity/docs/managing-endpoint-access), [reservation processing overview](https://developers.booking.com/connectivity/docs/reservations-api/reservations-overview), and [acknowledgement contract](https://developers.booking.com/connectivity/docs/reservations-api/acknowledging-new-reservations). The builders follow Booking.com's [availability](https://developers.booking.com/connectivity/docs/ota-hotelavailnotif) and [rate](https://developers.booking.com/connectivity/docs/ota-rateamountnotif) delta request contracts, but these fixtures are not provider certification.

## Expedia Group

No Expedia Group property-connectivity adapter is implemented. Expedia Group's lodging Connectivity Hub requires partner onboarding, license and PCI review, integration testing, and a site review before production use. See the [Connectivity Hub](https://developers.expediagroup.com/supply/lodging) and [getting started](https://developers.expediagroup.com/rapid/setup). The separate Rapid API is a traveler-facing booking product and is not a substitute for lodging supply connectivity.
